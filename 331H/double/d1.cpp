#include"Double.h"
#include<iostream>
#include<algorithm>
using namespace std;

//void displayMenu();

int main()
{

  Double l1;
  // l1.addBegin(1.2);
  // l1.addBegin(2.3);
  l1.addBegin(3.4);
  // l1.addBegin(4.5);
  // l1.addBegin(5.6);
  // l1.showforward();
  // l1.showbackward();
  l1.remove(1.2);
  l1.addBegin(4.5);
  // l1.remove(2.3);
  // l1.remove(3.4);
  // l1.remove(4.5);
  l1.showforward();
  // l1.remove(5.6);
  //

  // Double l2(l1);
  // // l2.addBegin(12.3);
  // // l1.showforward();
  // l2.showforward();
  // // l2.remove(12.3);
  //  l2.showbackward();

  // Double l3;
  // l3 = l2;
  // l3.addBegin(25.6);
  //
  // l2.showforward();
  // l3.remove(25.6);
  // l3.remove(5.6);
  // l3.remove(4.5);
  // l3.remove(3.4);
  // l3.remove(2.3);
  // l3.showforward();



}
